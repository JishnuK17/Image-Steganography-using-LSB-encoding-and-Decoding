# Image-Steganography-using-LSB-encoding-and-Decoding
LSB Image Steganography is a program that hides secret data inside an image file without visibly changing the image. It works by modifying the Least Significant Bits (LSB) of the image pixels to embed the secret message. The hidden data cannot be noticed by simply viewing the image.
